<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".matchMaking"></activity>
        <activity android:name=".externalService.importChat" />
        <activity android:name=".externalService.em_profile_list" />
        <activity android:name=".externalService.number_verification" />
        <activity android:name=".externalService.wUserSignup" />
        <activity android:name=".externalService.wUserLogin"/>
        <activity android:name=".login" />
        <activity android:name=".contactsList" />
        <activity android:name=".friendsList" />
        <activity android:name=".player_profile" />
        <activity android:name=".guess_emotion"
                  android:theme="@style/Theme.MaterialComponents.NoActionBar"/>
        <activity android:name=".guess_topic_of_discussion"
            android:theme="@style/Theme.MaterialComponents.NoActionBar"/>
        <activity android:name=".main_Menu" />
        <activity android:name=".SignUp" />
        <activity android:name=".homeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".gameInvitationService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".getDeviceToken">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>