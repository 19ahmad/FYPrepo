<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gameModule.splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".externalService.emotional_profile" />
        <activity android:name=".gameModule.matchMaking" />
        <activity android:name=".externalService.importChat" />
        <activity android:name=".externalService.em_profile_list" />
        <activity android:name=".externalService.number_verification" />
        <activity android:name=".externalService.wUserSignup" />
        <activity android:name=".externalService.wUserLogin" />
        <activity android:name=".gameModule.login" />
        <activity android:name=".gameModule.contactsList" />
        <activity android:name=".gameModule.friendsList" />
        <activity android:name=".gameModule.player_profile" />
        <activity android:name=".gameModule.guess_emotion" />
        <activity android:name=".gameModule.guess_topic_of_discussion"/>
        <activity android:name=".gameModule.main_Menu" />
        <activity android:name=".gameModule.SignUp" />
        <activity android:name=".gameModule.homeActivity">
        </activity>

        <service android:name=".gameModule.gameInvitationService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".gameModule.getDeviceToken">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>